import { TEXT, TEXT_MUTED, NAV_FONT } from '../theme';

export default function ProviderOnboarding() {
  return (
    <div style={{ padding: 30, fontFamily: NAV_FONT }}>
      <h1 style={{ color: TEXT }}>Provider Onboarding</h1>
      <p style={{ color: TEXT_MUTED, marginTop: 12 }}>This screen is being built. Check back soon.</p>
    </div>
  );
}
